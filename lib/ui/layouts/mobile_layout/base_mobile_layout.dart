import 'package:flutter/material.dart';

///@todo implement ui layouts for mobile
class BaseMobileLayout extends StatelessWidget {
  const BaseMobileLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
